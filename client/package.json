{
  "name": "wc2026-predictor-client",
  "private": true,
  "version": "1.0.0",
  "description": "Shim only — dependencies live in the repo root package.json. Do not run npm install here locally.",
  "scripts": {
    "postinstall": "npm install --include=dev --prefix ..",
    "build": "npm run build --prefix .."
  }
}
